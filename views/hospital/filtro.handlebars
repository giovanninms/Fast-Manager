<script>
    function detalheHospital() {
        window.location.href = "#";
    }
</script>

<div class=" h-100 ps-2 ">
    <div class="row w-100 mt-3 ps-2">
        <div class="col card">
            <div class="card-body">
                <div class="form">
                    <form action="/hospital/filtro" method="post">
                        <h5 class="row mt-4 ms-1">Fitrar Por:</h5>
                        <select name="selectFiltro" id="" class="form-control">
                            <option value="todos">Todos Hospitais</option>
                            <option value="nomeFantasia">Nome Fantasia</option>
                            <option value="cnpj">CNPJ</option>
                        </select>
                        <input type="text" name="inputFiltro" id="" class=" mt-2 form-control">
                        <button type="submit" class="btn btn-secondary mt-2">Filtrar</button>
                    </form>
                </div>
                <div class="buttons mt-4">
                    <a href="#" class="mt-2">Cadastrar Novo Hospital</a>
                    <a href="#"></a>
                </div>

            </div>
        </div>
        <div class="col-md-9">
            <div class="table-container">
                <div class="table-responsive " style="height: 30rem; overflow-y: auto;">
                    <table class="table table-hover table-bordered table-info table-sm">
                        <thead class="table-light">
                            <tr class="sticky-top">
                                <th>Nome Fantasia</th>
                                <th scope="col-md-auto">CNPJ</th>
                                <th scope="col-md-auto">Telefone</th>
                                <th scope="col-md-auto">Email</th>
                            </tr>
                        </thead>
                        <tbody>
                            {{#if hospital}}
                            <tr>
                                <td onclick="detalheHospital()">{{hospital.nomeFantasia}}</td>
                                <td onclick="detalheHospital()">{{hospital.cnpj}}</td>
                                <td onclick="detalheHospital()">{{hospital.celular}}</td>
                                <td onclick="detalheHospital()">{{hospital.email}}</td>
                                </a>
                            </tr>
                            {{else}}
                            <tr>
                                <td colspan="4">Nenhum hospital encontrado.</td>
                            </tr>
                            {{/if}}
                        </tbody>

                    </table>
                </div>
            </div>
        </div>
    </div>
</div>